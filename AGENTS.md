# 4-Agent 주식 분석 하네스

종목명 하나를 입력하면 4명의 AI 전문가가 순서대로 분석한 뒤 PDF로 텔레그램에 전송합니다.

---

## 사용법

```bash
python stock_agent_harness.py 삼성전자
python stock_agent_harness.py 005930
python stock_agent_harness.py TSLA
```

---

## 파이프라인 구조

```
입력 (종목명/코드)
    │
    ▼
[데이터 수집]
    ├─ DART API        → 3개년 재무제표 (매출, 영업이익, 부채비율 등)
    ├─ yfinance        → 현재가, PER, PBR, ROE, 섹터 정보
    └─ Naver 뉴스      → 최근 30일 기사 스크래핑
    │
    ▼
[에이전트 1] 재무분석가
    └─ 3년 매출 추이, 영업이익률, ROE, 부채비율, 유동비율
       → 재무 건전성 A/B/C/D 등급 + 리스크 요인
    │
    ▼
[에이전트 2] 뉴스 감성 분석가
    └─ 호재/악재 분류, 건수 집계
       → 시장 심리 긍정/중립/부정 판정
    │
    ▼
[에이전트 3] 업종 리서처
    └─ 글로벌 시장 흐름, 경쟁사 비교, 정책·규제 변화
       → 업종 전망 긍정/중립/부정
    │
    ▼
[에이전트 4] 투자 전략가 (공격적)
    └─ 위 3개 결과 종합
       → 적극 매수 / 분할 매수 / 관망 / 비중 축소 / 매도
          + 목표 수익률, 손절 라인, 진입 타이밍
    │
    ▼
PDF 저장 (analysis/) + 텔레그램 전송
```

---

## 현재 에이전트 목록

| # | 에이전트 | 역할 | 출력 |
|---|----------|------|------|
| 1 | 재무분석가 | DART + yfinance 데이터 해석 | A/B/C/D 등급 |
| 2 | 뉴스감성분석가 | 최근 1개월 뉴스 호재/악재 분류 | 긍정/중립/부정 |
| 3 | 업종리서처 | 글로벌 업종 트렌드 + 경쟁사 | 업종 전망 |
| 4 | 투자전략가 (공격적) | 1~3 종합 → 최종 판단 | 5단계 투자 판정 |

---

## 에이전트 추가 방법

`stock_agent_harness.py` 안에서 두 곳만 수정하면 됩니다.

### 1단계 — `PROMPTS` 딕셔너리에 시스템 프롬프트 추가

```python
PROMPTS = {
    # 기존 에이전트들...

    "리스크분석가": """당신은 투자 리스크 전문가입니다.
주어진 데이터를 바탕으로 다음을 분석하세요:
1. 시장 리스크 (섹터 전반)
2. 기업 고유 리스크 (재무, 경영진, 경쟁)
3. 거시경제 리스크 (금리, 환율, 정책)
각 리스크를 상/중/하로 등급화하고 한 줄 설명을 붙이세요.
모든 출력은 한국어로.""",
}
```

### 2단계 — `analyze()` 함수 파이프라인에 연결

```python
# 기존 에이전트 실행 다음에 추가
result_risk = run_agent("리스크분석가", f"종목: {name}\n재무: {result_fin}\n업종: {result_ind}")

# 전략가 입력에 포함
strategy_input = f"""
...기존 내용...

=== 리스크 분석가 결과 ===
{result_risk}
"""

# 반환 딕셔너리에 추가
return {
    ...
    "risk": result_risk,
    ...
}
```

### 3단계 — PDF 섹션에 추가

```python
sections = [
    ("📊 재무 분석가",          result["fin"]),
    ("📰 뉴스 감성 분석가",     result["news"]),
    ("🏭 업종 리서처",          result["industry"]),
    ("⚠️ 리스크 분석가",        result["risk"]),    # ← 추가
    ("⚡ 투자 전략가 (공격적)", result["strategy"]),
]
```

---

## 추가 가능한 에이전트 아이디어

| 에이전트 | 데이터 소스 | 역할 |
|----------|------------|------|
| 리스크분석가 | 재무 + 뉴스 | 시장/기업/거시 리스크 등급화 |
| 차트패턴분석가 | yfinance 가격 데이터 | 지지선/저항선, 이동평균, RSI |
| 배당분석가 | DART + yfinance | 배당수익률, 배당성향, 연속성 |
| 공매도분석가 | KRX 공매도 API | 공매도 비율, 대차잔고 추이 |
| ESG분석가 | 뉴스 + 공시 | ESG 이슈, 탄소중립 대응 |
| 내부자거래분석가 | DART 임원 공시 | 최대주주 매수/매도 동향 |

---

## 환경 설정 (.env)

```
GROQ_API_KEY=...          # 필수 — LLM 엔진
DART_API_KEY=...          # 한국 주식 재무제표
TELEGRAM_BOT_TOKEN=...    # PDF 전송
TELEGRAM_CHAT_ID=...      # 전송 대상
```

---

## LLM 모델 변경

`stock_agent_harness.py` 상단의 `GROQ_MODEL` 변수를 수정합니다.

```python
GROQ_MODEL = "llama-3.3-70b-versatile"   # 기본값 (균형)
GROQ_MODEL = "llama-3.1-8b-instant"      # 빠름, 비용 적음
GROQ_MODEL = "mixtral-8x7b-32768"        # 긴 컨텍스트 필요 시
```
